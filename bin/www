#!/usr/bin/env node
let winston = require('winston');
let config = require('../core/config');
let app = require('../app');


app.listen(config.get('PORT'),
    () => winston.info(`Server running at port ${config.get('PORT')}`));
